# Supabase (same as frontend)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional: for Phase 0.5 auth - validate Bearer JWT and set user for audit/matter isolation
SUPABASE_ANON_KEY=your-anon-key

# Server
PORT=3000
CORS_ORIGIN=http://localhost:4000

# Phase 1: Optional Redis for job queue (BullMQ). If unset, jobs run in-process.
# REDIS_URL=redis://localhost:6379

# Phase 1: Optional Apache Tika server for metadata extraction. If unset, uses Node (pdf-parse, mammoth).
# TIKA_SERVER_URL=http://localhost:9998

# Phase 9: AI-Assisted Review (embeddings + LLM). If unset, AI endpoints return errors.
# OPENAI_API_KEY=sk-...
# OPENAI_SUMMARIZE_MODEL=gpt-4o-mini
# OPENAI_SUGGESTIONS_MODEL=gpt-4o-mini
# AI_USAGE_CAP_PER_MATTER_PER_MONTH=10000
